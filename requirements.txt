# --- core bot / web ---
aiogram==3.15.0
fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1

# --- scheduling ---
apscheduler==3.11.0

# --- data / storage ---
asyncpg==0.30.0
sqlalchemy[asyncio]==2.0.36
alembic==1.14.0
redis==5.2.1

# --- integrations ---
gspread==6.1.4
google-auth==2.37.0
notion-client==2.2.1
feedparser==6.0.11

# --- media / pdf ---
# ffmpeg is a system binary (not pip). WeasyPrint needs GTK libs on Windows.
weasyprint==63.1

# --- config / logging ---
pydantic==2.9.2
pydantic-settings==2.6.1
loguru==0.7.3
python-dotenv==1.0.1
